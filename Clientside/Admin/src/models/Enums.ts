
enum SizeType {
    OneSize,
    Shoes,
    Shirt,
    Jackets,
    Pants,
    Shorts,
    Belt,
}

enum OrderStatus {
    New,
    Packing,
    Sent,
    Delivered,
    Cancelled,
}

export { SizeType, OrderStatus }